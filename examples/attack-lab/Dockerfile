# Example: a security lab. Just the base image - the whole point is to show what the
# UNMODIFIED sandbox does and does not allow. No extra packages, no ports, no services.
#
# After `outpost setup attack-lab`:
#   outpost sh attack-lab
# then run the checks in README.md and watch each one fail as expected.
FROM outpost-base:latest
