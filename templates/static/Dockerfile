# Static / frontend project (Vite, Astro, plain SPA).
#
# Node is installed per-project via fnm (see the node template's notes). This is the
# same as `node` but defaulted to a Vite-style dev port.
FROM outpost-base:latest

# Vite default; add others as needed (4321 Astro, 3000 preview, 6006 Storybook).
EXPOSE 5173
